# Leave this commented out until you have the real domain.
# A placeholder like https://your-domain.com splits canonical / og:url
# from the OG image host, and that mismatch ships into the sitemap too.
# Must include the protocol and no trailing slash.
# NUXT_PUBLIC_SITE_URL=https://example.com
