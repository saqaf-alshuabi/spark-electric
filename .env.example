# Cloudflare Pages URL until a custom domain exists.
# Also set these as Production environment variables in the Pages dashboard.
# Must include the protocol and no trailing slash.
NUXT_PUBLIC_SITE_URL=https://aboteem.pages.dev

# Optional: pin versions in the Pages dashboard if the build picks the wrong tools.
# NODE_VERSION=22
# PNPM_VERSION=11.21.0
